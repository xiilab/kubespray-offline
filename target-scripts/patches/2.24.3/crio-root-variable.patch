--- a/roles/container-engine/cri-o/templates/crio.conf.j2
+++ b/roles/container-engine/cri-o/templates/crio.conf.j2
@@ -7,7 +7,7 @@
 [crio]

 # Path to the "root directory". CRI-O stores all of its data, including containers images, in this directory.
-root = "/var/lib/containers/storage"
+root = "{{ crio_root | default('/var/lib/containers/storage') }}"

 # Path to the "run directory". CRI-O stores all of its state in this directory.
 # Read from /etc/containers/storage.conf first so unnecessary here
--- a/roles/container-engine/cri-o/defaults/main.yml
+++ b/roles/container-engine/cri-o/defaults/main.yml
@@ -1,5 +1,8 @@
 ---

+# CRI-O 데이터 저장 위치
+crio_root: "/var/lib/containers/storage"
+
 crio_cgroup_manager: "{{ 'systemd' if kube_kubeadm_cgroup_driver is defined and kube_kubeadm_cgroup_driver == 'systemd' else 'cgroupfs' }}"

 crio_conmon: "/usr/bin/conmon"
